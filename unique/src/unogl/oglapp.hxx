#include "oglapp.linux.hxx"
#include "oglapp.win32.hxx"

//tolua_begin
/**
namespace unogl{
  class GLAPP: public unogl::GRAPHCONTEXT{
    enum {
      dflt,
      none,
      bord,
      head,
      sbtn,
      maxw
    };
    
    unmath::vec2 pos;
    bool cursor;
    bool mode;
    unsigned int sync;
    unmath::vec3 gamma;
    
    GLAPP();
    ~GLAPP();
  };
}
**/
//tolua_end
