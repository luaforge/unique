
ifdef mode
  MODE=$(mode)
endif

ifdef platform
  PLATFORM=$(platform)
endif

ifdef compiler
  COMPILER=$(compiler)
endif

ifdef design
  DESIGN=$(design)
endif

ifdef debug
  DEBUG=$(debug)
endif

ifdef optim
  OPTIM=$(optim)
endif
